-- shiny.lua: Oferecer captura ou eliminação
-- Hook/Event: pokemon.caught (shiny check)
local Shiny = {}

function Shiny.init(mod, state)
  -- TODO: inicialização (Fase correspondente)
end

function Shiny.enable(mod, state)
  -- TODO: registrar hooks e events (Fase correspondente)
end

return Shiny
