-- accidental_death.lua: Morte durante captura bloqueia rota
-- Hook/Event: battle.fainted + encounter.roll
local Accidental_Death = {}

function Accidental_Death.init(mod, state)
  -- TODO: inicialização (Fase correspondente)
end

function Accidental_Death.enable(mod, state)
  -- TODO: registrar hooks e events (Fase correspondente)
end

return Accidental_Death
